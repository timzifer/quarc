{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/modbus/values.schema.json",
  "title": "Modbus Processor Values",
  "type": "object",
  "description": "Key/value map providing substitution data for configuration files.",
  "patternProperties": {
    "^.+$": {
      "description": "Arbitrary YAML node injected when referenced via !tag in configuration files.",
      "type": [
        "string",
        "number",
        "boolean",
        "null",
        "array",
        "object"
      ]
    }
  },
  "additionalProperties": false
}
